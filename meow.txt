# Initialize git repository
git init

# Add all files
git add .

# Create first commit
git commit -m "Initial commit: JIKI Twitter Crisis Detection project"

# Add your GitHub repo as remote
git remote add origin https://github.com/ihsankoolz/Tempo.git

# Push to GitHub
git branch -M main
git push -u origin main
